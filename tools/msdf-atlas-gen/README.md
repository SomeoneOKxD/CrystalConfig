# Local msdf-atlas-gen executable

Put the executable here:

```text
tools/msdf-atlas-gen/msdf-atlas-gen.exe
```

The Gradle task `:minecraft-mod:generateMsdfFonts` executes this file directly. It does not use PATH, CMD, environment variables, or IntelliJ terminal settings.
